[tool.poetry]
name = "app"
version = "0.1.0"
description = ""
authors = ["Tim"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pydantic = "^2.2.1"
pytest = "^7.4.0"
isort = "^5.12.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
